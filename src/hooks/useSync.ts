import { useCallback } from 'react';

export const useSync = () => {
    
    const syncUpData = useCallback(()=> {

    }, []);

    const syncDownData = useCallback(()=> {

    }, []);

    return
}